language: java
sudo: false
script: mvn clean verify

before_install:
  - git clone https://github.com/sosy-lab/cpachecker.git $CPAChecker
  - cd cpachecker
  - ant build
  - mkdir /tmp/cpacep
  - scripts/cpa.sh -spec config/specification/default.spc -config config/default.properties doc/examples/example.c -outputpath /tmp/cpacep -stats
  - pwd
  
