language: java
sudo: false
script: 
  - cd ..
  - cd ..
  - cd CPACEP
  - mkdir /tmp/test
  - mvn clean verify
  - ls /tmp/test
  
before_install:
  - cd ..
  - mkdir CPAChecker
  - cd CPAChecker
  - git clone https://github.com/sosy-lab/cpachecker.git
  - mkdir /tmp/original
  - cd cpachecker
  - ant build
  - scripts/cpa.sh -spec config/specification/default.spc -config config/default.properties doc/examples/example.c -outputpath /tmp/original -stats
